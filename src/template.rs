use crate::Result;

pub fn part1(input: &str) -> Result<i32> {
    Ok(0)
}

pub fn part2(input: &str) -> Result<i32> {
    Ok(0)
}

const INPUT: &str = "";

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test1() {
        let output = part1(INPUT);

        assert_eq!(0, output.unwrap());
    }

    #[test]
    fn test2() {
        let output = part2(INPUT);

        assert_eq!(0, output.unwrap());
    }
}

